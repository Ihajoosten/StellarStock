namespace StellarStock.Application.Handlers.Base
{
    /*public class GenericQueryHandler<TQuery, TResult> : IGenericQueryHandler<TQuery, TResult> where TQuery : IQuery<TResult>
    {
        public async Task<TResult> HandleAsync(TQuery query)
        {
            // Implement logic to handle the query and return result
            // Use repositories, services, etc., as needed
            Console.WriteLine($"Handling query of type {typeof(TQuery).Name}");
            return default; // Placeholder, replace with actual logic
        }
    }*/
}
